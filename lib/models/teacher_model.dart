class TeacherModel {
  String uid;
  String fullname;
  String email;
  String institutionID;
  String photo;
  String password;

  TeacherModel({required this.uid,required this.fullname, required this.email, required this.institutionID, required this.photo, required this.password});
}